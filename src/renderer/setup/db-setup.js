
import axios from 'axios'

export default (opt) => {
    // axios.interceptors.response.use(undefined, function (err) {
    //     const {status, statusText} = err.response
    //     return new Promise(function () {
    //         opt.onResponseErr({status, statusText})
    //     });
    // });    
    
}